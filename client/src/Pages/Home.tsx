import React from 'react';

interface HomeProps {
    // define your props here
  }
  
  const Home: React.FC<HomeProps> = (props) => {
    return (
        <div>
            <h1>Content</h1>
        </div>
    );
  };

export default Home;